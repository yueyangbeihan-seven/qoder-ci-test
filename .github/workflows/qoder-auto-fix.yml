name: Qoder Auto Fix

on:
  issues:
    types: [labeled]

jobs:
  qoder-auto-fix:
    if: contains(github.event.issue.labels.*.name, 'qoder-fix')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Qoder Auto Fix
        uses: QoderAI/qoder-action@v0
        with:
          qoder_personal_access_token: ${{ secrets.QODER_PERSONAL_ACCESS_TOKEN }}
          prompt: |
            /fix-issue
            REPO:${{ github.repository }}
            ISSUE_NUMBER:${{ github.event.issue.number }}
            ISSUE_TITLE:${{ github.event.issue.title }}
            ISSUE_BODY:${{ github.event.issue.body }}

# NOTE: Issue auto-fix is a GitLab CI-specific feature.
# This workflow is a placeholder for future GitHub Actions support.
# Currently, Qoder Action does not support automatic issue fixing on GitHub.
